Closes #

## What changed

One or two paragraphs: what was wrong or missing, what this PR does about it, and why this
shape rather than the obvious alternative.

## How I tested it

- [ ] `cd dashboard-ui && pnpm test && pnpm test:browser`
- [ ] `pnpm build`
- [ ] `bash -n bin/*.sh` and `python3 -m py_compile bin/*.py`
- [ ] Ran it against a real repository with `DRY_RUN=1` (say which flow you exercised)

## Checklist

- [ ] Commits follow `type(scope): imperative subject` with a prose body
- [ ] No company names, real hostnames, logins or tokens anywhere in the diff
- [ ] Docs updated if a knob, route, file or behaviour changed (`config.example`, `docs/`)
- [ ] The human-gated, COMMENT-only, post-as-yourself guarantees still hold

## Reviewer notes

Anything to look at first, or that you are unsure about.
